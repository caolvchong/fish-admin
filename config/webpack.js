// 生成页面的 title
exports.title = 'fish admin';

// 别名配置
exports.resolve = {
  alias: {
    fish: '@sdp.nd/fish'
  }
};

// todo: 静态资源路径配置, 如果有 cdn 支持，可以配置到 cdn，对本地开发无效
// exports.publicPath = './';

// 加入 vendor 公共包的库
exports.vendor = [];
